{
  "name": "missing required signal",
  "intent": {
    "intent_id": "demo",
    "intent_version": "1",
    "constraints": [
      {"id": "c_missing", "when": {"op": "eq", "left": {"var": "signals.safety"}, "right": "high"}, "effect": "deny"}
    ],
    "escalation": {"channels": ["policy"], "required_payload_fields": ["signal_request"]}
  },
  "proposed_action": {
    "schema_version": "0.1",
    "action_id": "write",
    "actor_id": "agent-7",
    "params": {}
  },
  "signals": {"schema_version": "0.1", "values": {}},
  "expected": {
    "decision": "ESCALATE",
    "must_include": ["reasons.triggered_rules", "escalation.channels"]
  }
}
