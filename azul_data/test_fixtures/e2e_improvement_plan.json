{
  "schema_version": "1.0.0",
  "generated_at": "2026-03-14T18:00:00Z",
  "source_system": "forgroot",
  "trigger": "threshold_breach",
  "requires_human_review": true,
  "prompt_diffs": [
    {
      "target_file": "sam/agents/failure_repair_agent.py",
      "additions": [
        "Before proposing a patch, state the blast radius: which units are directly affected and how many transitive dependents exist.",
        "Limit patch scope to the minimal set of files required. State explicitly if the repair cannot be contained to a single file."
      ],
      "removals": [],
      "examples": [
        "GOOD: 'Patch limited to sam/core/memory.py (1 direct unit, 3 transitive). No external dependency changes required.'",
        "BAD: 'Refactored memory module and updated all callers.' (over-scoped, blast radius unquantified)"
      ],
      "rationale": "FailureRepairAgent proposed over-scoped patches in 4 of last 10 cycles, causing unnecessary blast radius. Lead Agreement Rate for repair proposals dropped to 0.58 (threshold: 0.70)."
    }
  ],
  "contract_patches": [
    {
      "contract_name": "external.subprocess",
      "field_changes": {
        "risk_tier": 2,
        "guard_predicates": [
          "args[0] in ['ls', 'git status', 'whoami', 'python --version']",
          "kwargs.get('shell') == False"
        ]
      },
      "rationale": "subprocess.check_call with 'python --version' was rejected 3 times last month. Guard predicate is too restrictive for routine validation calls. Lowering risk_tier from 3 to 2 and expanding allowlist."
    }
  ],
  "policy_adjustments": [
    {
      "domain": "ci_change_control",
      "threshold_changes": {
        "reject_threshold": 0.25,
        "agreement_threshold": 0.75
      },
      "rationale": "ci_change_control reject rate has stabilised at 0.18 (well below old threshold of 0.30). Tightening threshold to 0.25 to catch emerging patterns earlier. Raising agreement threshold from 0.70 to 0.75 to reflect improved human-AI alignment on this domain.",
      "expected_impact": "Improvement recommendations will trigger earlier on rising reject rates. Agreement rate monitoring becomes stricter, surfacing minor divergences before they compound."
    }
  ],
  "distillation_batch": {
    "pair_count": 12,
    "avg_score": 0.91,
    "domains": ["ci_change_control", "system_operations"],
    "manifest_path": "azul_data/distillation_batches/batch_20260314_001.jsonl"
  }
}
